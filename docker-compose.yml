version: "3.3"

volumes:
  redis_data:

networks:
  default:
    driver: bridge

services:
  rest:
    build: .
    ports:
      - 80:8080
    environment:
      KUSSX_REDIS_HOST: redis
    links:
      - redis
  redis:
    image: redis
    volumes:
      - kussx_redis_data:/data


